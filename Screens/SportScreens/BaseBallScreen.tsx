import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const BaseBallScreen = () => {
  return (
    <View>
      <Text>BaseBallScreen</Text>
    </View>
  );
};

export default BaseBallScreen;

const styles = StyleSheet.create({});
